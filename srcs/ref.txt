-PWD
    use getcwd

-CD
    use chdir(CURRENT WORKING DIR/"INPUT")


(fork, execve[executes file])
    ls, echo, cd, setenv, unsetenv, env, exit

do {
    printf("> ");
    line = lsh_read_line();
    args = lsh_split_line(line);
    status = lsh_execute(args);

    free(line);
    free(args);
  } while (status);
}